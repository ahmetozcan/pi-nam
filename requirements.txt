# ---- PI-NAM: core requirements ----
# Python 3.10+ recommended

# Data
pandas>=2.0
numpy>=1.24
scipy>=1.11
pyyaml>=6.0
openpyxl>=3.1          # read the .xlsx dataset

# PI-NAM model (additive shape nets + differentiable physics) + parallel CV
torch>=2.0
joblib>=1.3

# Baselines (tuned via Optuna)
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
optuna>=3.0            # hyperparameter search (train-from-scratch)

# Figures
matplotlib>=3.7

# Interpretability (optional: SHAP on the full-feature benchmark)
shap>=0.44

# Optional: regenerate the TabPFN v2 baseline from scratch (CUDA strongly recommended).
# Not needed to reproduce results -- TabPFN is shipped as a frozen OOF array.
# Pinned to the exact version used in the paper:
# tabpfn==7.0.1
# torch==2.11.0     # (any recent torch works for PI-NAM; this is the version we used)
